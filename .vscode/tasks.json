{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Create package",
      "command": "vsce package -o ../out --allow-star-activation",
      "type": "shell",
      "group": "build",
      "options": {
        "cwd": "${workspaceFolder}/src/"
      }
    }
  ]
}